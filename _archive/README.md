# _archive

These files were moved here as part of the v2 storefront restructure.

They are **not** used by the production Next.js app and are kept only as
reference material. Do not import anything from this directory in app code.

| Entry | Reason archived |
|---|---|
| `fifth-avenue-prototype/` | Standalone Vite/React prototype used for layout exploration |
| `Downloaded_From_Stripe/` | Stripe boilerplate starter, superseded by `/app/api/payments/` |
| `src/` | Leftover Vite project scaffold (counter, main.js, settings.json) |
| `.tmp-mobile-check/` | Temporary mobile screenshot snapshots |
| `ClientEngine.jsx` | Duplicate of `components/ClientEngine.jsx` accidentally placed at root |
| `vite.config.js` | Vite config leftover, not used by Next.js |
| `cnf.js` | Tailwind config for the Vite prototype |
| `proxy.js` | Duplicate middleware file (Next.js middleware lives at `/middleware.js`) |
| `style.css` | Root-level CSS leftover from Vite scaffold |
| `index.html` | Vite entry HTML, not used by Next.js |
| `collections.html` | Static HTML prototype |
| `product.html` | Static HTML prototype |
| `home-mobile*.png` | Mobile device screenshots taken during development |
| `collections-mobile.png` | Mobile device screenshot |
